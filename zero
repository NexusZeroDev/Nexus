-- [[ NEXA HUB v11 - INFINITE REACH ]] --
print("LOADING NEXA HUB v11...")

-- 1. CLEANUP
if game.CoreGui:FindFirstChild("NexaHubUI") then
	game.CoreGui.NexaHubUI:Destroy()
end

-- 2. SERVICES
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local LocalPlayer = Players.LocalPlayer

-- 3. SETTINGS
local Settings = {
	AutoFarm = false,
	ChestFarm = false,
	FarmDistance = 15, -- WE CAN NOW STAY FARTHER AWAY (15 Studs)
	HitboxSize = 60,   -- How big the enemy hitbox becomes
	Smoothness = 0.3,
	IgnoredChests = {}
}

-- 4. GUI SETUP
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "NexaHubUI"
ScreenGui.Parent = game.CoreGui
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
MainFrame.Size = UDim2.new(0, 260, 0, 200) 
MainFrame.Position = UDim2.new(0.5, -130, 0.4, 0) 
MainFrame.Active = true
MainFrame.Draggable = true 

local Title = Instance.new("TextLabel")
Title.Parent = MainFrame
Title.Text = "NEXA HUB v11 (Reach)"
Title.TextColor3 = Color3.fromRGB(255, 50, 50)
Title.Font = Enum.Font.GothamBlack
Title.TextSize = 18
Title.Size = UDim2.new(1, 0, 0, 30)
Title.BackgroundTransparency = 1

local StatusLabel = Instance.new("TextLabel")
StatusLabel.Parent = MainFrame
StatusLabel.Text = "Status: Idle"
StatusLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
StatusLabel.TextSize = 12
StatusLabel.Size = UDim2.new(1, 0, 0, 20)
StatusLabel.Position = UDim2.new(0, 0, 0.85, 0)
StatusLabel.BackgroundTransparency = 1

-- 5. FUNCTION: HITBOX EXPANDER (THE MAGIC)
function ExpandHitbox(enemy)
	if not enemy then return end
	local HRP = enemy:FindFirstChild("HumanoidRootPart")
	if HRP then
		-- Make the enemy huge so your sword hits it from far away
		HRP.Size = Vector3.new(Settings.HitboxSize, Settings.HitboxSize, Settings.HitboxSize)
		HRP.Transparency = 0.7 -- Visible so you can see it working (set to 1 to hide)
		HRP.CanCollide = false
		HRP.Color = Color3.fromRGB(255, 0, 0)
	end
end

function AutoClick()
	-- 1. Virtual Click
	VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 1)
	task.wait(0.05)
	VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 1)
	
	-- 2. Tool Activate
	local Char = LocalPlayer.Character
	if Char then
		local Tool = Char:FindFirstChildOfClass("Tool")
		if Tool then Tool:Activate() end
	end
end

-- 6. BUTTONS
local FarmBtn = Instance.new("TextButton")
FarmBtn.Parent = MainFrame
FarmBtn.Size = UDim2.new(0, 200, 0, 40)
FarmBtn.Position = UDim2.new(0.5, -100, 0.25, 0)
FarmBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
FarmBtn.Text = "Start Reach Farm"
FarmBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
FarmBtn.Font = Enum.Font.GothamBold
FarmBtn.TextSize = 14
local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0, 6)
Corner.Parent = FarmBtn

FarmBtn.MouseButton1Click:Connect(function()
	Settings.AutoFarm = not Settings.AutoFarm
	if Settings.AutoFarm then
		FarmBtn.Text = "Stop Farm"
		FarmBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
	else
		FarmBtn.Text = "Start Reach Farm"
		FarmBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
		
		-- Reset Hitboxes when stopping
		for _,v in pairs(workspace:GetDescendants()) do
			if v:IsA("Humanoid") and v.Parent ~= LocalPlayer.Character then
				if v.Parent:FindFirstChild("HumanoidRootPart") then
					v.Parent.HumanoidRootPart.Size = Vector3.new(2, 2, 1) -- Reset size
					v.Parent.HumanoidRootPart.Transparency = 1
				end
			end
		end
	end
end)

-- 7. LOOPS

-- ATTACK LOOP
task.spawn(function()
	while true do
		if Settings.AutoFarm then
			pcall(function()
				AutoClick()
			end)
		end
		task.wait(0.15) -- Fast clicking
	end
end)

-- HITBOX & MOVEMENT LOOP
RunService.RenderStepped:Connect(function()
	if Settings.AutoFarm then
		local Char = LocalPlayer.Character
		if not Char or not Char:FindFirstChild("HumanoidRootPart") then return end
		local HRP = Char.HumanoidRootPart
		
		-- Auto Equip
		local Tool = Char:FindFirstChildOfClass("Tool")
		if not Tool then
			local BackpackTool = LocalPlayer.Backpack:FindFirstChildOfClass("Tool")
			if BackpackTool then BackpackTool.Parent = Char end
		end

		-- Find Enemy
		local ClosestDist = 1000
		local Target = nil
		local Folder = workspace:FindFirstChild("Enemies") or workspace:FindFirstChild("Mobs") or workspace
		
		for _, v in pairs(Folder:GetChildren()) do
			if v:FindFirstChild("Humanoid") and v:FindFirstChild("HumanoidRootPart") and v.Humanoid.Health > 0 then
				local Dist = (HRP.Position - v.HumanoidRootPart.Position).Magnitude
				if Dist < ClosestDist then
					ClosestDist = Dist
					Target = v
				end
			end
		end

		if Target then
			StatusLabel.Text = "Target: " .. Target.Name
			
			-- [[ APPLY REACH ]]
			ExpandHitbox(Target)
			
			local EnemyPos = Target.HumanoidRootPart.Position
			
			-- Teleport ABOVE the enemy (Safety) or Far away
			-- We use Settings.FarmDistance (set to 15 now)
			local TargetCFrame = CFrame.new(EnemyPos + Vector3.new(0, Settings.FarmDistance, 0), EnemyPos)
			
			HRP.CFrame = HRP.CFrame:Lerp(TargetCFrame, Settings.Smoothness)
			HRP.Velocity = Vector3.new(0,0,0)
			
			-- Noclip
			for _, part in pairs(Char:GetDescendants()) do
				if part:IsA("BasePart") then part.CanCollide = false end
			end
		else
			StatusLabel.Text = "Scanning..."
		end
	end
end)
